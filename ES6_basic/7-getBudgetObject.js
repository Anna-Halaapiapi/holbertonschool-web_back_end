// use shorthand syntax for key:variable name
export default function getBudgetObject(income, gdp, capita) {
  const budget = {
    // income: income,
    // gdp: gdp,
    // capita: capita,
    income,
    gdp,
    capita,
  };

  return budget;
}
