// adds a new attribute to a document in the collection school
db.school.update(
  { name: "Holberton school" }, // Filter to select the target document
  { $set: { address: "972 Mission street" } }, // Add the new attribute and its value
  { multi: true } // allow update of many docs (note - used for update which is now depricated)
);